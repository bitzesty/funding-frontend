<%=
  render partial: "partials/page_title",
         locals: {
             model_object: @funding_application,
             page_title: 'Get a payment or give a project update' 
         }
%>

<h1 class="govuk-heading-l">Select Journey</h1>

Controller acts as though both progress update and spend chosen

<%= form_tag(
    url: :funding_application_payment_request_start,
    method: :post,
    local: true
  ) do |f| 
%>

  <%= render(
      ButtonComponent.new(
        element: "button",
        text: t("buttons.labels.continue")
      )
    ) 
  %>

<% end %>