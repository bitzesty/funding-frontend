<%=
  render(
    partial: 'partials/page_title',
    locals: {
      model_object: @funding_application.open_medium,
      page_title: t('ownership.page_title')
    }
  )
%>

<noscript><% no_js = true %></noscript>

<div id="summary-errors"></div>

<%=
  render(
    partial: 'partials/summary_errors',
    locals: {
      form_object: @funding_application.open_medium,
      first_form_element: :open_medium_ownership_type_organisation
    }
  ) if @funding_application.open_medium.errors.any?
%>

<%=
  form_for @funding_application.open_medium,
  url: :funding_application_gp_open_medium_ownership,
  method: :put,
  remote: no_js ? true : false do |f|
%>

  <%=
    render(
      partial: 'partials/funding_application/ownership_question',
      locals: {
        model_object: @funding_application.open_medium,
        model_object_name: 'open_medium',
        form_object: f
      }
    )
  %>

  <%= render(ButtonComponent.new(element: 'input')) %>

<% end %>
