<% if @funding_application.open_medium.errors.any? %>

  document.title = "Error: <%== I18n.t('project_images.page_title') %>";

  removeAllFormGroupErrors(
    "project_image_files-form-group",
    "project_image_files-errors"
  );

  createSummaryErrorsSkeleton();

  var errorKeys = [];

  <% @funding_application.open_medium.errors.each do |error| %>

    errorKeys.push("<%= error.attribute.to_s %>");

    addSummaryError(
      false,
      "<%= error.attribute %>",
      "<%= error.message %>",
      "open_medium"
    );

    addFormGroupError(
      "<%= error.attribute %>-form-group",
      "<%= error.attribute %>-errors",
      "<%= error.attribute %>",
      "<%= error.message %>",
      "open_medium"
    );

  <% end %>

<% end %>
