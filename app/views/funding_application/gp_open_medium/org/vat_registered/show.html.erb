<%=
  render(
    partial: 'partials/page_title',
    locals: {
      model_object: @organisation,
      page_title: t('vat_registered.page_title')
    }
  )
%>

<%=
  render(
    partial: 'partials/summary_errors',
    locals: {
      form_object: @organisation,
      first_form_element: :organisation_vat_registered
    }
  ) if @organisation.errors.any?
%>

<%=
  form_with model: @organisation,
  url: :funding_application_gp_open_medium_vat_registered,
  method: :put,
  local: true do |f|
%>

  <%=
    render(
      partial: 'partials/organisation/vat_registered_question',
      locals: {
        model_object: @organisation,
        form_object: f
      }
    )
  %>

<%= render(ButtonComponent.new(element: 'input')) %>

<% end %>
