<%=
  render(
    partial: 'partials/page_title',
    locals: {
      model_object: @organisation,
      page_title: t('social_media.page_title')
    }
  )
%>

<%=
  render(
    partial: 'partials/summary_errors',
    locals: {
      form_object: @organisation,
      first_form_element: :organisation_social_media_info
    }
  ) if @organisation.errors.any?
%>

<%=
  form_with model: @organisation,
  url: :funding_application_gp_open_medium_social_media,
  method: :put,
  local: true do |f|
%>

  <%=
    render(
      partial: 'partials/organisation/social_media_question',
      locals: {
        model_object: @organisation,
        form_object: f
      }
    )
  %>

  <%= render(ButtonComponent.new(element: 'input')) %>

<% end %>
