<% if @funding_application.project.errors.any? %>

    document.title = "Error: <%== I18n.t('accounts.page_title') %>";

    removeAllFormGroupErrors(
        "accounts_files-form-group",
        "accounts_files-errors"
    );

    createSummaryErrorsSkeleton();

    var errorKeys = [];

    <% @funding_application.project.errors.each do |attr, msg| %>

        errorKeys.push("<%= attr.to_s %>");

        addSummaryError(
            false,
            "<%= attr %>",
            "<%= msg %>",
            "project"
        );

        addFormGroupError(
            "<%= attr %>-form-group",
            "<%= attr %>-errors",
            "<%= attr %>",
            "<%= msg %>",
            "project"
        );

    <% end %>

<% end %>
