<%=
  render partial: "partials/page_title",
         locals: {
             model_object: nil,
             page_title: I18n.t("dashboard.page_title")
         }
%>

<% if Rails.env.development? ||  Rails.env.staging? %>
  <p class = 'govuk-hint'> Your current user role is: <%= current_user.role%> </p>
<% end %>

<%= render 'dashboard/funding_applications' %>

<%= render 'dashboard/pa_project_enquiries' %>

<%= render 'dashboard/pa_expressions_of_interest' %>

<%= render 'dashboard/awarded_projects' %>
